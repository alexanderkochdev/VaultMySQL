database:
  url: "jdbc:mysql://127.0.0.1:3306/vaultmysql"
  user: "admin"
  password: "PASSWORD"

# Auto-migrate balances from EssentialsX flatfiles on first start.
# Only runs once – if the accounts table already has data, nothing happens.
migration:
  auto: true
